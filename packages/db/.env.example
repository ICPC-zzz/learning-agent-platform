# Copy this file to packages/db/.env for local Prisma schema validation.
# prisma validate only requires a syntactically valid DATABASE_URL; it does not connect to the database.
DATABASE_URL="postgresql://user:password@localhost:5432/learning_agent_platform?schema=public"
