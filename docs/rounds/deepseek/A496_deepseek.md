<!-- Generated by DeepSeek doc agent at 2026-06-26T11:30:26 -->

# A496 DeepSeek Handoff

## 1. 输入来源
- `docs/codex-context/CODEX_ALWAYS_READ.md`
- `docs/codex-context/CODEX_RULES.md`
- `docs/codex-context/CURRENT_HANDOFF.md`
- `docs/codex-context/SAFETY_BOUNDARIES.md`
- `docs/codex-context/ARCHITECTURE_BOUNDARIES.md`
- `docs/codex-context/DOC_WORKFLOW.md`
- `docs/rounds/codex/A496_codex.md`
- `docs/status/PROJECT_COMPLETION_SUMMARY.md`（用于进度基准验证）

## 2. 对 A496 Codex 完成情况的判断
- **本轮完成**：是，任务范围是修复 `apps/web` TypeScript 语义错误，目标从 308 降至小于 100。
- **完成内容**：
  - 修复了多个包（`ai-core`, `db`, `book-engine`, `learning-engine`, `shared`）的公共导出契约，补充 `memory` 模块、仓库类、类型定义等导出，以匹配 Web 端导入。
  - 调整了 `apps/web` 中 42 个文件的导入路径、类型适配、清理无效 `@ts-expect-error` 指令，修复数据形状和契约不匹配。
  - 把 `apps/web` 的 TypeScript 错误数从 **308** 降至 **94**，解析器级错误保持为 0，核心包 typecheck 维持 0 错误，dev server 正常。
- **新增/修改/移动文件**：
  - 修改核心包出口文件 8 个（见 A496_report §4）。
  - 修改 Web 端文件 37+ 个（详见 A496_report §4 的 Web 路径列表）。
  - 未移动任何文件。
- **验证状态**：
  - `pnpm -C apps/web typecheck`：错误数 94，符合目标（<100）。
  - 四个核心包 typecheck 全绿。
  - `http://localhost:3000` HTTP 200 正常。
  - 无 Git 写入，工作区保持 dirty。
- **风险或未完成事项**：
  - 剩余 94 个错误仍主要为数据形状/契约不匹配（82 个）、空值/未知值推断过窄（7 个）、缺失模块或私有导出（4 个）、隐式 any（1 个）。
  - 本次修改了包公共导出，可能影响其他未覆盖的引用；类型检查已通过，但运行时行为未经浏览器或端到端验证。
  - 工作区已有大量未提交脏文件，未隔离本轮的包修改。

## 3. 与长期项目状态的关系
- **新增完成能力**：无新增业务功能。本轮仅为类型系统修复，使 `apps/web` typecheck 从 308→94，恢复编译基线，为后续业务开发消除障碍。
- **仍未完成能力**：所有此前在 PROJECT_COMPLETION_SUMMARY 中列出的未完成能力（Reader 真实同步、Auth v2、Desktop 集成、Agent 真实能力等）全部不变。
- **preview-only / mock-only / disabled-by-default 能力**：本轮末触碰任何真实 AI 调用、工具执行、Agent loop 或数据库写入；所有安全边界保持原样。
- **是否建议更新 PROJECT_COMPLETION_SUMMARY.md**：不建议更新。本轮无功能增量，项目总进度维持 61.00%，无需触发压缩或重新估算。

## 4. 当前安全边界
- 本轮仅修复 TypeScript 类型，未使用任何真实 LLM provider、工具执行、Agent loop、数据库迁移或网络调用。
- 未保存 raw prompt / raw response，未输出或硬编码 API key、数据库密码、token、secret。
- 所有 AI、Tool、Skill 能力仍为 preview-only / mock-only / disabled-by-default。
- UI 中任何涉及 AI 的组件仍应标注“开发预览”。
- 后续业务开发必须继续遵守 SAFETY_BOUNDARIES.md 和 CODEX_RULES.md。

## 5. 下一轮 Codex 唯一任务建议
**A497：修复 `apps/web` 中 `app/books` 目录下的类型错误，使其归零（或降至 3 个以内）。**

- 当前 `app/books` 目录剩余 17 个 error（主要集中于 `manage/actions.ts` 和 `delete-book-actions.ts`）。
- 任务范围：只修改 `apps/web/src/app/books/` 下的文件以及必要的包公共导出（如 `BookRepository` 返回契约），目标是将 `app/books` 区域 error 清零。
- 禁止扩大到其他目录或重构业务逻辑。
- 修复后必须运行 `pnpm -C apps/web typecheck`，确认 `app/books` 范围归零且整体错误数减少。

## 6. 下一轮 Codex 必读文件（最多 10 个）
1. `docs/codex-context/CODEX_ALWAYS_READ.md`
2. `docs/codex-context/CODEX_RULES.md`
3. `docs/codex-context/CURRENT_HANDOFF.md`
4. `docs/codex-context/SAFETY_BOUNDARIES.md`
5. `docs/codex-context/ARCHITECTURE_BOUNDARIES.md`
6. `docs/codex-context/DOC_WORKFLOW.md`
7. `docs/rounds/codex/A496_codex.md`

（若修复 books 时涉及 reader 或 import 类型关联，可随时按需读取对应文件，但默认不读历史轮次。）

## 7. 下一轮 Codex 禁止范围
- **禁止读取** `E:\code\harness-main`
- **禁止读取** `E:\code\ccx`
- **禁止读取** `E:\code\claude-desktop-app-main`
- **禁止读取**全部历史 Axxx 文档（除 `A496_codex.md` 外）
- **禁止删除**项目文件（只允许重写现有文件内容，禁止 `rm`、`del`、`git rm` 等删除操作）
- **禁止执行**真实 provider / tools / agent loop，除非任务明确要求
- **禁止修改**无关业务代码（只改 `app/books` 及其必要的包出口）
- **禁止执行**任何 Git 写入命令（`add`、`commit`、`push`、`stash`、`reset`、`restore` 等）
- **禁止**启动新业务功能或新增 UI 组件
- **禁止**保存或输出 raw prompt / raw response、密钥或数据库密码

## 8. 给 ChatGPT 生成下一轮 Codex 提示词的备注
ChatGPT 在生成 A497 提示词时必须强调：
- 任务是纯类型修复，不增加任何业务功能。
- 只允许修改 `apps/web/src/app/books/` 及必要的包公共导出，禁止扩范围。
- 明确引用 A496 遗留的 books 区域 17 个错误，以 `pnpm -C apps/web typecheck` 当前报告为准。
- 要求每修复一组立即 typecheck 验证，最终目标使 `app/books` 区域 error 归零，但对整体 94 errors 不做严格要求。
- 边界：禁止真实 AI/工具调用，禁止 Git 写入，禁止读取三个外部参考项目。

## 9. 项目总进度估算
**项目总进度：61.00%**

口径：Web 网页端 + 软件端/Desktop 为主线，Skill 社区仅占位。本轮仅修复 type，无新增能力，进度不变。
