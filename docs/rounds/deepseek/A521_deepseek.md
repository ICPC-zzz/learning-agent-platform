<!-- Generated by DeepSeek doc agent at 2026-06-29T23:36:43 -->

# A521 DeepSeek Handoff

## 1. 输入来源
- `docs/rounds/codex/A521_codex.md`
- `docs/codex-context/CURRENT_HANDOFF.md`
- `docs/codex-context/CODEX_ALWAYS_READ.md`
- `docs/codex-context/CODEX_RULES.md`
- `docs/codex-context/SAFETY_BOUNDARIES.md`
- `docs/codex-context/ARCHITECTURE_BOUNDARIES.md`
- `docs/codex-context/DOC_WORKFLOW.md`
- `docs/status/PROJECT_COMPLETION_SUMMARY.md`（阶段参考）

## 2. 对 A521 Codex 完成情况的判断

### 本轮是否完成
**已完成。** A521 成功修复 Web 生产构建链 P0，并通过生产模式浏览器冒烟测试，所有预设路由正常。

### 完成内容
- 根目录新增真实 `build` 脚本，依赖顺序为 `db build → web build`。
- `apps/web` 新增 `build` (`next build --no-lint`) 和 `start` (`next start`) 脚本。
- `tsconfig.base.json` 移除与 Next 生产类型校验冲突的 `verbatimModuleSyntax`，保留 `isolatedModules`。
- `pnpm run build` 通过，生成 `apps/web/.next` 和 `packages/db/dist`。
- 生产服务启动 (`pnpm --filter @learning-agent-platform/web start`) 成功。
- @Browser 在生产模式下检查 8 条预设路由，覆盖桌面 `1440x900` 和移动 `390x844`，无致命控制台错误、无图片/字体加载失败，404 与 admin guard 行为正确。

### 新增/修改/移动文件
- 新增脚本项（`package.json` 修改）：根目录 `package.json`、`apps/web/package.json`。
- 修改 `tsconfig.base.json`：移除 `verbatimModuleSyntax`。
- 未移动任何文件。

### 验证状态
- `pnpm run typecheck`（全量）通过。
- 生产构建 `pnpm run build` 通过。
- 生产启动 `pnpm start`（web）正常。
- 浏览器生产模式冒烟无致命错误。
- 已有测试（A515~A518）通过；A520/A521 无对应测试文件。
- Scoped ESLint（`eslint.config.mjs`）通过。

### 风险或未完成事项
- **Auth v2 / 生产会话**未实现，仍为 dev session/OTP-preview。
- **浏览器 E2E 自动化测试**缺失。
- **管理员授权态**生产 Browser 工作流未单独 QA。
- **内容调度器/scheduler/cron**未完成。
- **AI provider**仍为 dev-only/preview-only，不能宣称生产可用。
- **Codeforces 账户绑定同步**生产 Browser 流未闭合。
- **全仓历史 ESLint 债**仍存在（A521 仅保证生产构建链和 scoped lint）。
- **Desktop 端**继续禁止（`desktopEntryAllowed = false`）。

## 3. 与长期项目状态的关系

### 新增完成能力
- **Web 生产构建链**从零到完整可用，打通了根构建、db 构建、web 构建、生产启动验证和浏览器生产模式冒烟测试。

### 仍未完成能力
- Auth v2 / 生产 session
- 浏览器 E2E 自动化测试覆盖
- 管理员角色端到端验证
- 文章实时数据管道
- Codeforces 实时 API 同步
- AI 真实模型接入
- 桌面客户端集成
- 内容调度与 cron 部署

### preview-only / mock-only / disabled-by-default 能力
- 所有 Agent/Tool/Provider/Runtime/Skill 能力仍保持 preview-only / mock-only / disabled-by-default。
- UI 标记“开发预览”，未更改任何能力上线状态。

### 是否建议更新 PROJECT_COMPLETION_SUMMARY.md
**暂不建议。** A521 仅补齐生产构建基础设施，未新增面向终端用户的功能闭环，当前进度 46% 已反映实际情况。待 Auth v2 或 E2E 门禁完成后再统一阶段压缩。

## 4. 当前安全边界
- 本轮**未调用**任何真实 LLM provider，**未执行**任何真实工具。
- 未保存 raw prompt/response。
- 未泄露或硬编码 API key、数据库密码、token 或 secret。
- 生产构建未执行 prisma migration、`db push` 或 reset。
- Desktop 入口继续显式禁止。
- 所有 preview/mock/dev-only 能力边界维持不变。

## 5. 下一轮 Codex 唯一任务建议
**实现 Web 的生产 Auth v2，或建立浏览器 E2E 自动化门禁（二选一）。** 推荐优先推进 Auth v2，因为它是后续管理员工作流、用户绑定和 AI 真实调用的前置依赖。任务应限定范围：不涉及其他模块，不引入真实 LLM 调用，不解除 Desktop 门禁。

## 6. 下一轮 Codex 必读文件
1. `docs/codex-context/CODEX_ALWAYS_READ.md`
2. `docs/codex-context/CODEX_RULES.md`
3. `docs/codex-context/CURRENT_HANDOFF.md`
4. `docs/codex-context/SAFETY_BOUNDARIES.md`
5. `docs/codex-context/ARCHITECTURE_BOUNDARIES.md`
6. `docs/codex-context/DOC_WORKFLOW.md`
7. `docs/rounds/codex/A521_codex.md`
8. `docs/status/A521_WEB_PRODUCTION_BUILD_CLOSURE.md`（构建状态）
9. `docs/status/A521_WEB_PRODUCTION_BROWSER_QA.md`（浏览器 QA 报告）
10. `docs/status/A520_DESKTOP_ENTRY_GATE.md`（Desktop 门禁）

## 7. 下一轮 Codex 禁止范围
- 禁止读取 `E:\code\harness-main`
- 禁止读取 `E:\code\ccx`
- 禁止读取 `E:\code\claude-desktop-app-main`
- 禁止读取全部历史 Axxx 文档（除已列为必读的 A521）
- 禁止删除项目文件
- 禁止真实 provider / tools / agent loop，除非任务明确要求且安全边界已补齐
- 禁止修改 `apps/desktop`
- 禁止解除 Desktop 门禁
- 禁止修改 Prisma schema、执行 migration 或 reset
- 禁止顺手清理全仓 lint 或无关业务问题

## 8. 给 ChatGPT 生成下一轮 Codex 提示词的备注
- **强调**：本轮唯一主线任务是补齐 Web Auth v2 或建立 E2E 自动化门禁，二选一，不可同时开展。
- 必须明确当前所有 Agent/AI 能力仍为 preview-only / disabled，不可在此任务中打开真实 LLM 调用。
- 必须沿用现有的 dev session 机制作为临时登录验证手段，但实现的生产 Auth 必须可演进至真实 session 管理。
- 所有新增能力必须在构建、生产启动后，通过 @Browser 进行基本功能冒烟，但不需要全量 E2E（除非本轮选择的就是 E2E 门禁）。
- 任务完成后必须更新 `CURRENT_HANDOFF.md`，并在交接中明确指出 Auth/E2E 门禁的闭合状态。
- 禁止触碰 `apps/desktop`，禁止解除 Desktop 入口限制。
- 必须声明当前项目总进度为 46%，本轮完成后可根据实际闭合程度小幅上调（1~3%）。

## 9. 项目总进度估算
**项目总进度：46.00%**

Web 生产构建链已打通，但面向用户的真实闭环（认证、数据实时性、AI 调用、E2E 验证、桌面集成）仍大规模缺失，因此进度与 A521 前持平。待下一轮 Auth v2 或 E2E 门禁实现后，再根据完成度评估提升。
