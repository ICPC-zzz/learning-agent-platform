<!-- Generated by DeepSeek doc agent at 2026-05-21T11:26:06 -->

# A134 DeepSeek Handoff

## 1. 输入来源

- `docs/status/PROJECT_COMPLETION_SUMMARY.md`
- `docs/codex-context/CODEX_ALWAYS_READ.md`
- `docs/codex-context/CODEX_RULES.md`
- `docs/codex-context/CURRENT_HANDOFF.md`
- `docs/codex-context/SAFETY_BOUNDARIES.md`
- `docs/codex-context/ARCHITECTURE_BOUNDARIES.md`
- `docs/codex-context/DOC_WORKFLOW.md`
- `docs/rounds/codex/A134_codex.md`

## 2. 对 A134 Codex 完成情况的判断

- **本轮是否完成**：✅ 已完成。A134 是一次纯文档工程轮次（docs‑only），目标全部达成。
- **完成内容**：
  - 建立了 `docs/codex-context` 小上下文目录，包含 Codex 每轮默认读取规则、安全边界、架构边界、三段式文档工作流和短交接文档。
  - 新增 `docs/status/PROJECT_COMPLETION_SUMMARY.md` 作为长期完成度总结。
  - 生成 `docs/rounds/codex/A134_codex.md` 作为本轮总结。
  - 保留 `docs/rounds/deepseek/.gitkeep` 占位。
  - 更新 `docs/codex-context/CURRENT_HANDOFF.md` 作为留给下一轮 Codex 的短交接。
  - 更新 `docs/README.md` 默认入口指向小上下文。
  - 迁移旧 `docs/codex-tasks/CODEX_RULES.md` 至新位置并保留兼容说明。
  - 将阶段性路线图、数据流文档移入 `docs/_archive_pending_review/` 等待人工复核。
- **新增/修改/移动文件**：
  - 新增 8 个文件（参见 `A134_codex.md` 第 3 节）。
  - 修改 3 个文件（`CODEX_RULES.md`、旧位置兼容说明、`README.md`）。
  - 移动 3 个文件（旧规则迁移、两份阶段文档归档）。
- **验证状态**：已执行 `git status`、目录列表、敏感词搜索等，确认本轮变更仅限 `docs/` 目录，无敏感信息泄露，结构符合预期。未运行 typecheck/lint 因为 docs‑only 范围不涉及业务代码。
- **风险或未完成事项**：
  - A131～A133 轮次总结缺失，项目进度只能保守基于 A128～A130 估算。
  - `docs/codex-tasks/DEVELOPMENT_ROADMAP.md` 仍保留旧位置，未纳入新工作流，建议后续人工决策。
  - 本轮纯文档整理，未推进任何业务功能，项目产品能力 0 增长。

## 3. 与长期项目状态的关系

- **新增完成能力**：
  - 项目协作与文档工程能力从零散的单文档工作流升级为三段式低上下文工作流，Codex 每轮只需读取少量固定文档。
  - 长期完成度总结文件已建立，后续阶段压缩可以直接更新。
- **仍未完成能力**（业务侧）：
  - Web MVP 最短闭环仍未完整可验收，reader 的阅读进度恢复、真实章节问答、每日题单反馈闭环、URL/文件/PDF/EPUB 导入等均未落实。
  - 真实 LLM provider、真实工具执行、真实 Agent loop、真实 Skill 执行均未启用。
  - Desktop Agent 完全未实现。
- **仍是 preview‑only / mock‑only / disabled‑by‑default 的能力**：  
  完整的清单见 `SAFETY_BOUNDARIES.md`，包括 Agent 工作台、工具需求/风险、LLM 调用、reader QA、Skill manifest/registry/suggest 等。
- **是否建议更新 `PROJECT_COMPLETION_SUMMARY.md`**：  
  暂时不建议。A134 已经在 `PROJECT_COMPLETION_SUMMARY.md` 第 4 节末尾和第 5 节末尾被记录，本次深交接无新增业务事实，无需重复更新。等到后续业务轮次有实质性推进时再从 DeepSeek 侧发起更新。

## 4. 当前安全边界

- 真实 LLM provider、真实工具执行、真实 Agent loop、真实 Skill 执行、后台任务 **仍不得默认开启**。
- `/agent` 所有 runtime / tool / memory / skill 相关视图仍为 preview‑only 或 mock‑only。
- reader QA、RAG、embedding、vector search 未形成产品闭环，均视为 mock / preview / provider‑gated。
- **禁止**保存或输出 raw prompt、raw response。
- **禁止**泄露或硬编码任何 API key、数据库密码、token、secret。
- UI 中不得将 preview/mock/disabled 能力宣传为已上线能力。

## 5. 下一轮 Codex 唯一任务建议

**修复 Web reader 阅读进度保存后刷新页面仍能恢复进度的功能**。

- 当前 reader 已能“在 demo 用户边界下保存阅读进度”，但可能未在页面刷新时正确恢复已保存进度。
- 任务仅限在现有 `apps/web` reader 页面、`packages/db` reading progress repository 和 server actions 内做最小修复，确保：
  - 用户进入 reader 时自动加载最近一次保存的章节/进度。
  - 刷新页面后进度不丢失。
- 验证：本地启动 Web app，导入一本书，进入 reader，拖动进度或翻页保存，刷新页面，确认进度停留在上次位置。
- 此任务紧贴 Web MVP 最短闭环，不涉及任何 Agent、RAG、provider，完全符合安全边界。

## 6. 下一轮 Codex 必读文件

最多 10 个（按优先级）：

1. `AGENTS.md`
2. `docs/codex-context/CODEX_ALWAYS_READ.md`
3. `docs/codex-context/CODEX_RULES.md`
4. `docs/codex-context/CURRENT_HANDOFF.md`
5. `docs/codex-context/SAFETY_BOUNDARIES.md`
6. `docs/codex-context/ARCHITECTURE_BOUNDARIES.md`
7. `apps/web/src/app/reader/` 相关页面/组件（reader 入口）
8. `packages/db` 中 `ReadingProgress` 相关 repository
9. `apps/web/` 中与 reader progress 交互的 server action / API 路由
10. `docs/codex-context/DOC_WORKFLOW.md`（如对工作流有疑问）

## 7. 下一轮 Codex 禁止范围

- ❌ 禁止读取外部参考源码目录：`E:\code\harness-main`、`E:\code\ccx`、`E:\code\claude-desktop-app-main`
- ❌ 禁止读取所有历史 `Axxx_codex.md` / `Axxx_deepseek.md` 文档（仅本轮 A134 相关文档通过 DeepSeek 交接，Codex 不应直接读取）
- ❌ 禁止删除项目文件（即使觉得冗余，只能建议移动至 `docs/_archive_pending_review/`）
- ❌ 禁止真实 LLM provider 调用
- ❌ 禁止真实工具执行
- ❌ 禁止真实 Agent loop
- ❌ 禁止修改 `packages/ai-core`、`packages/book-engine`、`packages/learning-engine`、`packages/shared` 中与任务无关的代码
- ❌ 禁止修改 `apps/web` 中与 reader progress 无关的页面（如 `/agent`、`/books` 列表逻辑等）
- ❌ 禁止开启任何 Skill、MCP、Desktop 或其他超前功能

## 8. 给 ChatGPT 生成下一轮 Codex 提示词的备注

- 下一轮提示词必须明确这是 **A135**。
- 明确任务范围仅为 `apps/web` 的 reader progress 恢复，并让 Codex 知晓该进度已由 `packages/db` 提供保存能力。
- 提示词必须包含：
  - “**唯一任务**：修复 reader 页面刷新后阅读进度丢失，使其能正确恢复最近一次保存的进度。”
  - “**允许修改文件**：`apps/web` 内 reader 相关组件/页面/actions，`packages/db` 中 reading progress repository（如确有必要）。”
  - “**验证标准**：本地 `npm run dev`，导入书籍→进入 reader→手动保存进度→刷新页面，确认进度正确恢复。”
  - “**禁止范围**：禁止修改 `/agent`、`/learning`、`/books` 列表等功能；禁止触摸 ai‑core、book‑engine、learning‑engine 等包。”
  - “**安全边界**：不得保存 raw prompt/response，不得使用真实 LLM，不得执行工具，不得泄露密钥。”
  - 要求 Codex 先阅读 `AGENTS.md` 和小上下文文档。
- 最后强调：项目当前处于 Web MVP 原型阶段，所有 Agent / Tool / Skill 能力仍为 preview/mock/disabled，本任务无需考虑这些模块。

## 9. 项目总进度估算

项目总进度：**22.00%**

（保持 A128 审计至 A134 文档工程后的保守估算，本轮深交接未增加业务能力，不调整百分比）

---

## 附：CURRENT_HANDOFF.md 更新草案

根据本次 DeepSeek 交接，建议将 `docs/codex-context/CURRENT_HANDOFF.md` 更新为以下内容（替换原文件），以确保下一轮 Codex 获得正确的业务指引：

```markdown
# Current Handoff

A134 已完成 docs 文档工作流整理，A134_deepseek 已生成。下一轮 Codex（A135）开始业务开发。

## 下一轮任务

**唯一任务**：修复 Web reader 阅读进度保存后刷新页面仍能恢复进度的功能。

## 下一轮必读文件

- `AGENTS.md`
- `docs/codex-context/CODEX_ALWAYS_READ.md`
- `docs/codex-context/CODEX_RULES.md`
- `docs/codex-context/SAFETY_BOUNDARIES.md`
- `docs/codex-context/ARCHITECTURE_BOUNDARIES.md`
- `apps/web` 下 reader 相关组件与 server actions
- `packages/db` 中 reading progress 相关 repository

## 下一轮禁止读取

- 所有历史 Axxx 文档（包括 codex 和 deepseek）
- `docs/status/PROJECT_COMPLETION_SUMMARY.md`（除非任务明确要求）
- `E:\code\harness-main`、`E:\code\ccx`、`E:\code\claude-desktop-app-main`

## 安全边界（重申）

- 真实 LLM provider、真实工具执行、真实 Agent loop、真实 Skill 执行仍不得默认开启。
- preview‑only、mock‑only、disabled‑by‑default 的能力不能写成真实上线能力。
- 禁止保存或输出 raw prompt / raw response / secret。

## 文档工作流提醒

- 本轮完成后 Codex 需生成 `docs/rounds/codex/A135_codex.md` 并更新本文件。
- 后续 DeepSeek 将根据 `A135_codex.md` 生成 `A135_deepseek.md`。
```
