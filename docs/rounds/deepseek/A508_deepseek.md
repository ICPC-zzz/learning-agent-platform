<!-- Generated by DeepSeek doc agent at 2026-06-27T16:31:00 -->

# A508 DeepSeek Handoff

## 1. 输入来源
- `docs/rounds/codex/A508_codex.md`
- `docs/codex-context/CURRENT_HANDOFF.md`（含 A508 追加段）
- `docs/status/PROJECT_COMPLETION_SUMMARY.md`
- `docs/codex-context/CODEX_ALWAYS_READ.md`
- `docs/codex-context/CODEX_RULES.md`
- `docs/codex-context/SAFETY_BOUNDARIES.md`
- `docs/codex-context/ARCHITECTURE_BOUNDARIES.md`
- `docs/codex-context/DOC_WORKFLOW.md`

## 2. 对 A508 Codex 完成情况的判断
- **本轮是否完成**：是，完成了预设的 Codeforces 个性化助手闭包任务。
- **完成内容**：
  - 新增 `codeforces-personalized-provider.ts` 及三个 CF 辅助工具（`resolveLearnerTrainingProfile`、`getPersonalizedCodeforcesCandidates`、`getUpcomingCodeforcesContests`），并在 assistant orchestrator 中实现确定性 CF 意图路由。
  - 训练推荐优先使用最新有效学习报告估算的实际 rating，候选题目仅从本地 curated Codeforces 池读取，即将到来比赛从官方 `contest.list` 获取（支持缓存，不混用历史比赛数据）。
  - Assistant 响应消息携带 `usedTools`、`sources`、`toolTimeline` 证据，前端 AI 聊天面板渲染工具时间线。
  - 学习报告、复习计划、代码分析摘要被归类为**只读学习上下文**，不占用用户长期记忆管理界面。
  - 所有新增/改动通过全栈 typecheck（0 错误），并通过 6 个测试套件及 A508 专项测试，真实 Codeforces API smoke 通过。
- **新增/修改/移动文件**：
  - 新增：`apps/web/src/lib/assistant/learning-artifact-classification.ts`、`apps/web/src/lib/assistant/providers/codeforces-personalized-provider.ts`、`tests/a508-cf-personalized-agent.test.mjs`、`docs/status/A508_CF_PERSONALIZED_AGENT_CLOSURE.md`、`docs/rounds/codex/A508_codex.md`
  - 修改：assistant orchestrator、server actions、types、memory-service、tools（codeforces-tools/tool-registry/tool-types）、learning-artifact-memory、cf-contest-service、codeforces-client、AssistantChatPanel、AssistantMemoryOverviewPanel、CURRENT_HANDOFF.md
- **验证状态**：
  - 全量 typecheck 通过。
  - 相关测试套件全部通过（含 A508 专项测试）。
  - 真实 CF API 烟雾测试通过。
- **风险或未完成事项**：
  - 所有 Agent/Tool/LLM 调用仍为 preview-only / mock-only / disabled-by-default，A508 新增的 CF 工具及路由仍运行在 mock 沙箱中，未接通真实 LLM。
  - 未执行 Prisma schema 迁移，未涉及数据库写操作。
  - 学习报告、复习计划等仅为只读上下文，未建立持久化 artifact 仓库。
  - 未进行 git 提交。

## 3. 与长期项目状态的关系
- **新增完成能力**（视为进展，但保持 mock 边界）：
  - 基于用户真实学习等级（报告估算 rating）的 CF 训练推荐。
  - 本地 curated 池的个性化候选题目查询。
  - 官方未来比赛信息的获取与缓存。
  - AI 聊天中展示工具调用证据，提升透明度和可调试性。
  - 学习产物（报告/计划/分析）明确归类为非用户记忆，避免 UI 混淆。
- **仍未完成能力**：
  - 真实 LLM 接入（AI 所有响应仍是 mock）。
  - 真实 CF 实时数据管线（题目、比赛、用户统计仍未常态化同步）。
  - 真实用户认证（Auth v2）。
  - 文章模块的真实数据管道。
  - 桌面端业务集成。
  - 学习分析和复习计划的端到端真实验证。
- **preview-only / mock-only / disabled-by-default 能力**：
  - 所有 Agent、Tool、Provider、Skill 运行时能力仍完全处于上述状态，本次未改变。
- **是否建议更新 PROJECT_COMPLETION_SUMMARY.md**：
  - 暂不建议。A508 为单一功能轮次，未达成阶段性里程碑，且整体 mock 状态未突破。可在下一轮阶段压缩（预计 8~12 轮后）一并吸收。

## 4. 当前安全边界
- 所有 Agent、Tool、Provider、Skill 能力仍为 **preview-only / mock-only / disabled-by-default**。
- 未进行任何真实 LLM 调用，未执行真实工具，未保存 raw prompt/response。
- 未泄漏或硬编码任何 API key、数据库密码、token、secret。
- Codeforces 工具虽然通过真实 API 烟雾测试，但在 assistant 产品流程中仍使用 mock 提供者，不实际发起对外请求。
- 学习记忆与上下文仅向当前登录 session 提供安全摘要，无跨用户数据暴露。
- UI 中已清晰标明“预览”“模拟”等状态，未将 mock 能力描述为真实上线功能。

## 5. 下一轮 Codex 唯一任务建议
**更新 A500 遗留的 4 个过时 source-check 测试，使断言与 A498–A508 当前产品界面一致。**
- 原因：4 个测试（Problems 元数据、curated pool 边界、AI Assistant 路径、User 页面权限）目前 FAIL，属于长期技术债务；修复不触及架构，仅调整断言与已稳定页面对齐，风险低。
- 验收：所有现有测试套件通过，且新增/修改的测试断言明确匹配当前产品文档与页面行为。
- 备选：若已有授权执行 DB schema 修改，可改为将学习分析/复习计划报告持久化至 DB artifact repository。

## 6. 下一轮 Codex 必读文件
1. `AGENTS.md`
2. `docs/codex-context/CODEX_ALWAYS_READ.md`
3. `docs/codex-context/CODEX_RULES.md`
4. `docs/codex-context/CURRENT_HANDOFF.md`
5. `docs/rounds/codex/A508_codex.md`
6. `docs/rounds/deepseek/A508_deepseek.md`（本文件）
7. `docs/status/PROJECT_COMPLETION_SUMMARY.md`（若不熟悉 A500 测试缺口，可选择性阅读相关段）
8. `docs/codex-context/SAFETY_BOUNDARIES.md`
9. `docs/codex-context/ARCHITECTURE_BOUNDARIES.md`
10. 相关测试文件：`tests/a479-codeforces-problems-metadata.*`、`tests/a484-curated-pool-and-agent-candidates.*`、`tests/a462-ai-assistant-page.*`、`tests/user/page-source.*`（如存在）

## 7. 下一轮 Codex 禁止范围
- 禁止读取 `E:\code\harness-main`
- 禁止读取 `E:\code\ccx`
- 禁止读取 `E:\code\claude-desktop-app-main`
- 禁止读取全部历史 Axxx 文档（除指定上述最近轮次）
- 禁止删除任何项目文件（包括源码、文档、测试）
- 禁止执行真实 LLM provider、真实工具调用、真实 Agent loop，除非任务明确要求并单独审批
- 禁止修改与测试更新无关的业务代码、架构、数据库 schema
- 禁止进行无边界重构或扩大模块范围

## 8. 给 ChatGPT 生成下一轮 Codex 提示词的备注
- 强调本轮任务为**纯测试修复**，只能修改测试文件及辅助断言工具，不能触碰任何业务逻辑。
- 需明确列出 4 个过时测试文件及各自失败原因（参考 `PROJECT_COMPLETION_SUMMARY.md` 的 Test Status 表）。
- 要求完成修改后执行全量 typecheck 并运行所有测试套件，给出通过/失败清单。
- 重申安全边界：不调用真实 API，不写数据库，不删除文件，不读取禁止目录。
- 提示如果遇到无法通过且需大幅改动测试逻辑的情况，应立即停止并报告，不要自行扩大范围。

## 9. 项目总进度估算
**项目总进度：32.00%**

依据：
- 上一轮（A507）结束后 CURRENT_HANDOFF 记录为 31.00%（新口径：仅计入真实数据管线可用功能）。
- A508 新增了基于用户学习等级的训练推荐、本地池候选、未来比赛查询及工具证据展示功能，这些功能在 UI 层面已可演示（虽然仍处于 mock 环境），提升了助手实用性的完整度。
- 因仍无真实 LLM 接入、无真实 CF 数据管线、无 Auth v2，进度提升有限，给予 +1.00% 增量。

---

# 文档清理计划

## 扫描范围
基于当前已知的文件清单和归档规则生成的初步建议。由于未在实际环境中扫描 `docs/` 目录，以下建议需用户人工确认并补充。

## 保留文档
- `docs/product/PRODUCT_SPEC.md`
- `docs/architecture/SYSTEM_ARCHITECTURE.md`
- `docs/reference-analysis/` 下 Harness / CCX / Claude Code / memory / tools 分析文档
- `docs/status/PROJECT_COMPLETION_SUMMARY.md`
- `docs/codex-context/` 下所有小上下文文档（CODEX_ALWAYS_READ.md, CODEX_RULES.md, CURRENT_HANDOFF.md, SAFETY_BOUNDARIES.md, ARCHITECTURE_BOUNDARIES.md, DOC_WORKFLOW.md）
- 最近 3~5 轮 Codex 总结：`docs/rounds/codex/A504_codex.md` 至 `A508_codex.md`（假设存在）
- 最近 3~5 轮 DeepSeek 交接：`docs/rounds/deepseek/A504_deepseek.md` 至 `A508_deepseek.md`
- 当前轮新增的状态文档：`docs/status/A508_CF_PERSONALIZED_AGENT_CLOSURE.md`（暂保留直到下一轮阶段压缩）

## 建议归档文档
（由于未获得完整目录树，以下为常见可归档类型，需用户确认实际文件是否存在）
- 已被压缩吸收的旧轮次文档（如 A394–A403 原始轮次，若尚未归档）
- `docs/status/A394-A499_STAGE_SUMMARY.md`（若已被 PROJECT_COMPLETION_SUMMARY.md 替代，但仍有压缩参考价值，可保留至下次压缩后归档）
- 旧路径兼容文档（如早期 reader/oj 相关设计草案）
- 被 A508 状态文档取代的临时 handoff 副本（若存在）
- 已迁移到 codex-context 的原 `docs/codex-tasks/` 下旧任务文档

## 禁止归档
- 所有上述“保留文档”
- 任何源代码文件
- `docs/_archive_pending_review/` 内的文件（等待用户人工删除，不应重复归档）

## 需要用户确认
1. 实际 `docs/rounds/codex/` 和 `docs/rounds/deepseek/` 中是否有 A504 之前的大量历史轮次文件未被压缩，需要手工归档。
2. 是否存在 `docs/` 根目录下散落的旧设计草案（如 `reader-*-design.md`、`reader-*-audit.md` 等），若有，建议归档。
3. `docs/status/A394-A499_STAGE_SUMMARY.md` 是否仍有被 CURRENT_HANDOFF 引用的必要，若无，可归档。
4. 任何用户手动放入的临时文档。

**清除动作必须使用 `git mv` 移入 `docs/_archive_pending_review/`，并等待用户人工删除，严禁直接删除文件。**

## 清理计划 JSON
```json
{
  "retain": [
    "docs/product/PRODUCT_SPEC.md",
    "docs/architecture/SYSTEM_ARCHITECTURE.md",
    "docs/reference-analysis/*",
    "docs/status/PROJECT_COMPLETION_SUMMARY.md",
    "docs/codex-context/CODEX_ALWAYS_READ.md",
    "docs/codex-context/CODEX_RULES.md",
    "docs/codex-context/CURRENT_HANDOFF.md",
    "docs/codex-context/SAFETY_BOUNDARIES.md",
    "docs/codex-context/ARCHITECTURE_BOUNDARIES.md",
    "docs/codex-context/DOC_WORKFLOW.md",
    "docs/rounds/codex/A504_codex.md",
    "docs/rounds/codex/A505_codex.md",
    "docs/rounds/codex/A506_codex.md",
    "docs/rounds/codex/A507_codex.md",
    "docs/rounds/codex/A508_codex.md",
    "docs/rounds/deepseek/A504_deepseek.md",
    "docs/rounds/deepseek/A505_deepseek.md",
    "docs/rounds/deepseek/A506_deepseek.md",
    "docs/rounds/deepseek/A507_deepseek.md",
    "docs/rounds/deepseek/A508_deepseek.md",
    "docs/status/A508_CF_PERSONALIZED_AGENT_CLOSURE.md"
  ],
  "suggest_archive": [
    "docs/rounds/codex/A394-A403 (if not yet archived)",
    "docs/status/A394-A499_STAGE_SUMMARY.md (if superseded)",
    "docs/reader-*-design.md (any legacy drafts)",
    "docs/reader-*-audit.md (any legacy audits)",
    "docs/codex-tasks/* (if migrated to codex-context)"
  ],
  "forbidden_archive": [
    "docs/product/PRODUCT_SPEC.md",
    "docs/architecture/SYSTEM_ARCHITECTURE.md",
    "docs/status/PROJECT_COMPLETION_SUMMARY.md",
    "docs/codex-context/*",
    "docs/_archive_pending_review/*",
    "docs/reference-analysis/*"
  ],
  "user_confirmation_needed": [
    "Verify existence of legacy round files < A504",
    "Confirm archiving of A394-A499_STAGE_SUMMARY.md",
    "Check for root-docs legacy drafts",
    "Inspect any manually added temp files"
  ]
}
```
