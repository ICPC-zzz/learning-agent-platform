<!-- Generated by DeepSeek doc agent at 2026-06-26T17:45:43 -->

# A500 DeepSeek Handoff

## 1. 输入来源
- `docs/status/PROJECT_COMPLETION_SUMMARY.md`（A500 更新版）
- `docs/codex-context/CODEX_ALWAYS_READ.md`
- `docs/codex-context/CODEX_RULES.md`
- `docs/codex-context/CURRENT_HANDOFF.md`
- `docs/codex-context/SAFETY_BOUNDARIES.md`
- `docs/codex-context/ARCHITECTURE_BOUNDARIES.md`
- `docs/codex-context/DOC_WORKFLOW.md`
- `docs/rounds/codex/A500_claude.md`

## 2. 对 A500 Codex 完成情况的判断
- **本轮已完成**：是。
- **完成内容**：执行了一次大规模的文档压缩与清理，包括：
  - 创建 7 份新文档：文档清单、清理清单、阶段压缩总结（A394–A499）、废弃模块说明、精简文档索引、重写后的`PROJECT_COMPLETION_SUMMARY.md`、本轮总结。
  - 更新 2 份已有文档：`CODEX_ALWAYS_READ.md`、`CURRENT_HANDOFF.md`。
  - 删除 66 个 `docs/` 下的过期/重复文档（旧轮次、旧方向设计等）。
  - 删除 207 个临时日志/诊断文件（`.codex_tmp` 与根目录 `.log`），总计释放约 7.5 MB。
  - 备份所有删除文件至 `E:\code\lap-a500-doc-cleanup-backup-20260626-092953`。
- **验证状态**：所有保留文档路径存在性验证通过；文档交叉引用无断链；未触碰任何业务源码、数据或 Git 历史；无法在当前环境运行 typecheck，但 A499 已确认 0 errors 且本轮无源码变更。
- **风险或未完成事项**：无风险。**明确的未完成事项**：4 项源检查测试断言已过期，需要更新以符合当前产品范围（见 A501 建议）。

## 3. 与长期项目状态的关系
- **新增完成能力**（文档治理层面）：
  - 项目总进度口径正式切换至“AI Native 学习平台四模块”，核心汇总文档 `PROJECT_COMPLETION_SUMMARY.md` 已重写。
  - 旧模块（书库/Reader/OJ 等）已文档化归入 `LEGACY_MODULES.md`，并明确恢复保护规则。
  - A394–A499 跨轮阶段已压缩为 `A394-A499_STAGE_SUMMARY.md`，避免未来上下文膨胀。
  - 精简的文档索引 `DOCUMENT_INDEX.md` 已建立。
- **仍未完成能力**（功能层面）：与 A499 一致：
  - 真实 Auth v2、真实 CF API 回归、文章真实数据管线、AI 模型真实接入等关键缺口未动。
  - 4 项源检查测试过期，尚未修复。
- **preview-only / mock-only / disabled-by-default 能力**：所有 Agent/Tool/Provider/Skill/Runtime 能力维持原边界，未变动。
- **是否建议更新 PROJECT_COMPLETION_SUMMARY.md**：已在 A500 本轮完成重写并归档，无需重复更新。

## 4. 当前安全边界
所有 Agent / Tool / Provider / Skill / Runtime 能力仍为 **preview-only / mock-only / disabled-by-default**。  
本轮及未来任何轮次：
- 不执行真实 LLM provider 调用。
- 不执行真实工具。
- 不保存 raw prompt/response。
- 不泄露任何 API key、数据库密码、token 或 secret。
- 所有 DB 写入仍依赖多层 `LAP_*` 环境变量 guard，默认关闭。
- UI 必须明确标注“开发预览”。

## 5. 下一轮 Codex 唯一任务建议
**A501：更新 4 项过期的源检查测试，使断言对齐当前产品实现。**

具体要求（小步、明确）：
1. 修复 `a479-codeforces-problems-metadata.test.mjs`：断言对齐当前 CF 元数据页面结构，而非旧组件分工。
2. 修复 `a484-curated-pool-and-agent-candidates.test.mjs`：调整 targetSize 断言以匹配 `full-pool-v2` 实现。
3. 修复 `a462-ai-assistant-page.test.mjs`：修正路径拼接错误，确保可正常执行。
4. 修复 `user/page-source.test.mjs`：适配当前 `/user` 为可见预览（不再要求未登录强制跳转）。
- 规则：**严禁回滚当前产品行为**来通过测试；只需更新断言。
- 完成后运行所有包 typecheck，保持 0 errors。
- 此任务为小范围代码修正，完成后应回到功能开发主线。

## 6. 下一轮 Codex 必读文件
1. 本次 `A500_deepseek.md`（本交接文档）。
2. `docs/codex-context/CURRENT_HANDOFF.md`
3. `docs/rounds/codex/A500_claude.md`
4. `docs/status/PROJECT_COMPLETION_SUMMARY.md`
5. `docs/status/LEGACY_MODULES.md`
6. 需要修复的 4 个测试源文件：
   - `apps/web/../a479-codeforces-problems-metadata.test.mjs`
   - `apps/web/../a484-curated-pool-and-agent-candidates.test.mjs`
   - `apps/web/../a462-ai-assistant-page.test.mjs`
   - `apps/web/../user/page-source.test.mjs`
   （具体路径以项目实际结构为准，Codex 可自行定位）

## 7. 下一轮 Codex 禁止范围
- **禁止读取** `E:\code\harness-main`、`E:\code\ccx`、`E:\code\claude-desktop-app-main`。
- **禁止读取**全部历史 Axxx 轮次文档（超出本次指定的范围）。
- **禁止删除**任何项目文件（仅允许修改测试断言代码）。
- **禁止调用**真实 LLM provider、真实工具执行、真实 agent loop（本次任务为纯测试修复）。
- **禁止修改**无关业务代码、Prisma schema、环境变量、页面路由等。
- **禁止执行** `git add/commit/push`、`prisma db push` 等写入操作。
- 不得将 mock/preview 能力描述为真实上线。

## 8. 给 ChatGPT 生成下一轮 Codex 提示词的备注
ChatGPT 生成下一轮提示词时需明确强调：
- 任务范围仅限 4 个测试文件的断言修复，保持产品行为不变。
- 必须在修改前阅读本次 deepseek handoff 和 `CURRENT_HANDOFF.md` 以获取上下文。
- 不得回滚 `/user`、`/problems` 等页面的现有产品行为以迎合过期测试。
- 修复完成后验证 typecheck 保持 0 errors。
- 若因路径或依赖问题无法在当前环境运行测试，仅做静态代码修复并注明验证限制。
- 安全边界依然生效：不调用真实 LLM、不保存 raw prompt。

## 9. 项目总进度估算
**项目总进度：45.00%**

（口径：仅含 Web 网页端 + 软件端/Desktop 的主线四模块；AI 能力因全部 mock 不计为完整功能；文章模块无真实数据管线计为部分完成。此值为 45–50% 区间的保守下界，由 A500 重写的 `PROJECT_COMPLETION_SUMMARY.md` 明确给出。）
