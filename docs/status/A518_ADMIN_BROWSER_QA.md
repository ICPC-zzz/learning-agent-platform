# A518 Admin Browser QA

Date: 2026-06-28

## @Browser 调用情况

- 已按本轮要求使用 `browser:control-in-app-browser`。
- 浏览器目标：`http://127.0.0.1:3110`。
- dev server 启动时服务端设置 `LAP_ADMIN_USER_IDS=dev-user-001`。

## 后台浏览器验收

- 首页当前管理员 session 显示“后台”入口。
- 直访 `/admin` 成功进入后台，没有 403/404。
- 后台侧栏不存在“书籍管理”和“导入管理”。
- 后台侧栏保留：管理概览、题目资源、内容同步、AI 助手、系统设置。
- 后台同步页基础文字可读，状态和错误信息可读。

## 内容同步浏览器验收

- `/admin/sync` 存在三个独立按钮：
  - 刷新每日热点
  - 刷新 GitHub 日报
  - 刷新技术文章
- 每日热点按钮真实点击成功，状态更新为同步成功。
- GitHub 日报按钮真实点击成功，状态更新为同步成功。
- 技术文章按钮真实点击后进入 disabled/loading，完成后解除 loading，状态更新为同步成功。
- 技术文章最终显示当前 1866 篇，本次新增 2 篇。

## 文章页面浏览器验收

- `/articles` 正常渲染。
- 页面显示每日热点、GitHub 日报、博客园和 CSDN。
- 页面显示博客园 1811、CSDN 55，总计与后台技术文章同步结果一致。
- 热点快照显示最近同步时间。
- GitHub 项目数显示为 39。

## 普通用户验证

- 无 cookie 首页 HTTP 200，页面 HTML 不包含“后台”入口。
- 无 cookie 访问 `/admin` 返回 404。
- 未用第二个真实浏览器账号完成角色切换。

## 控制台状态

- `tab.dev.logs({ levels: ["error", "warning", "warn"] })` 返回空数组。
- Browser 插件自身出现过 Statsig 网络超时日志，不属于本地应用页面控制台。

## 用户仍需复验

- 如需验证真实多账号切换，请使用非管理员账号登录后复验“后台”入口消失与 `/admin` 拒绝。
