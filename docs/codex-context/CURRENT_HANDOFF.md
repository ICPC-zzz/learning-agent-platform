<!-- Generated by DeepSeek doc agent at 2026-05-23T16:08:06 -->

# CURRENT_HANDOFF

## 1. 当前状态
- **主线范围**：Web 网页端 + Desktop 软件端；Skill 社区仅占位/scaffold，不纳入近期完成度。
- **Agent/Tool/LLM/Runtime**：一律 preview‑only / mock‑only / disabled‑by‑default，无真实调用。
- **Reader 本地能力**：已实现滚动位置恢复、阅读计时、已读标记、进度百分比、章节导航、可见块指示器、Demo 横幅、笔记草稿、字体调节、最近访问章节记录、阅读时长统计面板、本地书签/标记等多项客户端预览能力。`ReadingProgress` 表同步做最小 DB 同步，localStorage 兜底，均标注“开发预览”。
- **Desktop**：最小 Electron 骨架（安全配置就绪，依赖已安装），但 GUI 未能在 VM 启动（需用户在 Windows 桌面手动验证）。
- **遗留问题**：约 40 个文档清理导致的 deleted/modified 文件仍未暂存/提交；`pnpm-workspace.yaml` 已创建，需用户在 Windows 先清理残留 symlink 再 `pnpm install` 才能正常启动 Desktop。

## 2. 最近一轮结果（A177）
- **任务**：Reader 纯客户端书签/标记功能。
- **操作**：新增 `ReaderBookmarksPanel.tsx`（localStorage CRUD、书签列表、跳转、删除、清空，上限 30 条），集成到 `page.tsx` 右侧栏，补充 `globals.css` 样式。
- **验证**：typecheck 零新增错误（仅遗留 22 个 node_modules 错误），lint 15/15 全绿，浏览器验收需用户在 Windows 手动执行。
- **安全**：无 API key/密码泄露，未调用真实 AI，UI 标注“开发预览”。

## 3. 下一轮 Codex 必读
- `docs/codex-context/CODEX_ALWAYS_READ.md`
- `docs/codex-context/CODEX_RULES.md`
- `docs/codex-context/CURRENT_HANDOFF.md`（本文件）
- `docs/codex-context/SAFETY_BOUNDARIES.md`
- `docs/codex-context/ARCHITECTURE_BOUNDARIES.md`
- `docs/codex-context/DOC_WORKFLOW.md`

## 4. 下一轮 Codex 禁止
- 把 preview / mock / disabled 能力描述为真实上线能力。
- 真实调用 LLM provider 或执行真实工具（除非用户显式开启独立任务）。
- 超范围修改 Skill 社区实现。
- 连续多轮只做文档清理（压缩 / 归档由 DeepSeek 负责）。
- 顺手修无关问题或展开无边界重构。

## 5. 安全边界
- Agent / Tool / Provider / Runtime 一律 mock‑only / disabled‑by‑default。
- Reader Ask AI 固定 mock + network disabled；Desktop 仅本地静态 HTML。
- 严禁硬编码 API key、密码、token、secret。
- 进度同步仅保存非敏感状态，UI 标注“开发预览”。

## 6. 文档处理分工
- **Codex**：每轮输出 `Axxx_codex.md` 并更新本交接文档，不做大范围历史压缩或归档。
- **DeepSeek**：定期将多轮成果压缩进 `PROJECT_COMPLETION_SUMMARY.md`、生成清理计划、建议归档。
- 如需清理文档，只移动至 `docs/_archive_pending_review/`，等待用户人工删除。

## 7. 下一步唯一建议
从以下选项选一项小而明确的任务（A178）：
1. **Desktop Electron GUI 启动验证**（用户在 Windows 桌面手动执行 `pnpm --filter @learning-agent-platform/desktop dev`，确认窗口和页面）。
2. **将遗留文档的 modified/deleted 文件 `git add` 并提交**，清理工作区。
3. **Reader 其他本地能力扩展**（如自定义书签 label、书签编辑、导出等）。

## 8. 项目总进度
**项目总进度：约 36.00%**（Web + Desktop 主线口径，截至 A177）。  
Reader 已具备多项本地预览能力及最小 DB 同步；Agent 真�