<!-- Generated by Claude Code at 2026-06-23 -->

# CURRENT_HANDOFF

## 1. 当前状态

**主线**：Web 网页端 AI Native 学习平台（文章中心、CF 题目训练、AI 助手）为主；Desktop 仅有空白壳；Skill 社区仅占位/scaffold，不计入进度分母。

已建成核心能力：
- 用户认证、Codeforces Handle 服务端绑定、个人中心 SVG 看板
- 文章中心三 Tab（每日技术热点、GitHub 日报、技术文章）
- Codeforces 精选题中心（2000 题、最小元数据、安全合规）
- Agent Runtime v1 统一执行底座（类型/注册/权限/执行）
- CF 学习分析 Agent v3（Rating 预估、复习计划、比赛推荐，默认关闭）
- AI 助手模型配置系统 v1（凭据 AES-256-GCM 加密、SSRF 防护、用户默认模型接入 `/ai` 真实对话）
- A491：单轮代码分析与 Debug 闭环 v1（`/ai` 粘贴代码+题目 → 模型分析 → 结构化安全报告）
- **A492：用户画像驱动的简单多 Agent 代码学习分析闭环 v1**（题目画像 + CF 学习画像 + 难度匹配 + 薄弱标签匹配 + 个性化建议 + 后续训练题推荐 + Agent 事件时间线）

## 2. 最近一轮结果（A492）

**用户画像驱动的简单多 Agent 代码学习分析闭环 v1（代码已交付，含分析历史持久化 + CF 共享缓存）**

核心交付（累计）：
- `packages/ai-core/src/code-analysis/a492-types.ts` — A492 完整类型定义
- `packages/ai-core/src/code-analysis/problem-profiling.ts` — 题目画像 Skill
- `packages/ai-core/src/code-analysis/difficulty-fit.ts` — 难度匹配 + 弱标签匹配纯函数
- `packages/ai-core/src/code-analysis/tools/cf-user-tools.ts` — 6 个 AgentTool
- `packages/ai-core/src/code-analysis/personalized-orchestrator.ts` — 固定 8 步多 Agent 编排器（含过期上下文保护）
- `apps/web/src/app/ai/CodeAnalysisPanel.tsx` — 新增 Rating/标签/CF 开关输入
- `apps/web/src/app/ai/A492PersonalizedReport.tsx` — 8 Section 报告 + Agent 时间线
- `apps/web/src/app/ai/code-analysis-actions.ts` — A491/A492 双路由 + 自动保存历史
- `apps/web/src/app/ai/cf-tool-adapters.ts` — CF 工具适配器（使用 /user 页同款算法 + 共享缓存）
- `apps/web/src/app/ai/analysis-history-actions.ts` — 分析历史持久化（基于 MemoryItem 表）
- `apps/web/src/app/ai/AnalysisHistoryPanel.tsx` — 分析历史面板（带时间戳、可删除）
- `apps/web/src/lib/cf-computation-cache.ts` — /user 和 /ai 共享的 CF 计算缓存（5 分钟 TTL）
- `apps/web/src/app/user/cf-learning-analysis-action.ts` — 集成共享缓存，避免重复计算
- `tests/a492-personalized-analysis.test.mjs` — 12 组 69 项测试全部通过

## 3. 下一轮 Codex 必读

- `docs/codex-context/` 下 6 个核心文件
- `docs/rounds/codex/A492_claude.md`（本轮总结）
- `docs/reference-analysis/CCX_MEMORY_AND_TOOLS_ANALYSIS.md`
- `packages/ai-core/src/code-analysis/` 所有文件
- `packages/ai-core/src/code-analysis/personalized-orchestrator.ts`
- `packages/ai-core/src/code-analysis/a492-types.ts`
- `apps/web/src/app/ai/A492PersonalizedReport.tsx`
- `apps/web/src/app/ai/CodeAnalysisPanel.tsx`
- `apps/web/src/app/ai/code-analysis-actions.ts`
- `apps/web/src/app/ai/cf-tool-adapters.ts`

## 4. 下一轮 Codex 禁止

- 修改 Prisma 中 ModelProvider/Credential/Profile 模型结构
- 修改 Credential Vault 加密算法或密钥派生方式
- 关闭 SSRF 防护或放宽连接策略
- 将 Secret 写入 localStorage、客户端 state 或日志
- 实现文件上传、代码编译/运行、自由多 Agent 对话
- 复制 CCX 源码
- 修改 CF Rating 核心算法或精选题池
- 绕过 Tool 权限
- 保存 Raw Prompt/Response

## 5. 安全边界

所有 Agent / Tool / Skill 仍为 **preview-only / disabled-by-default**。CF Learning Agent 默认关闭，仅服务端控制。凭据加密存储，SSRF 防护生效，永不保存原始 Prompt/Response。

禁止描述任何能力为"生产上线"；禁止绕过 Repository/安全层/权限校验。

## 6. 文档处理分工

- **Codex**：写代码、修 bug、跑验证；每轮输出 `Axxx_codex.md` 并更新本文件
- **DeepSeek**：定期将多轮进展压缩进 `PROJECT_COMPLETION_SUMMARY.md`，清理过期文档

## 7. 下一步唯一建议

**A493：AI 助手会话、结构化报告和运行记录持久化 v1。**

将 A491/A492 的一次性不保存结果升级为可持久化查看的会话和运行记录：
- Conversation / Message / AgentRun / AgentStep / Report 模型
- 不保存 Raw 模型 Response
- 支持查看历史记录、删除和重新运行
- 用户隔离
- 前端真实验收

不要同时实现文件上传、记忆中心和自动压缩。

## 8. 项目总进度

项目总进度：**61.00%**

（新口径：Web 真实可用学习闭环 + Agent Runtime + CF 学习分析 Agent v3 + AI 模型配置接入真实 LLM + 单轮代码分析闭环 v1 + 用户画像驱动多 Agent 代码分析 v1；Skill 社区不计入。A492 代码交付，待用户前端验收。）
