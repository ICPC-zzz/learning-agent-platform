<!-- Generated by Claude Code + DeepSeek (A157+) at 2026-05-22 -->

# CURRENT_HANDOFF

> 由 A157+ 补救后更新，供 Codex 下一轮优先读取

## 1. 当前状态
- 主线范围：Web 网页端 + Desktop 客户端。Skill 社区仅占位，不参与进度计算。
- Web 端各页面（/books、/import、/reader、/learning、/agent）已完成安全边界审查与预览/演示标注。
- **Reader 页面顶部 DemoModeNotice 横幅已完成并提交**（A157 commit: `eecda3f`，A157+ 确认收尾: `docs/rounds/codex/A157+_claude.md`）。横幅为纯静态 UI，明确告知用户当前为预览状态、进度仅章节级、AI/RAG/provider 未启用。
- Desktop 客户端不可启动。
- 所有 Agent / Tool / LLM provider / Skill / Runtime 能力保持 preview-only / mock-only / disabled-by-default。
- `packages/ai-core` 代码层面已通过 A156 清理并提交，无遗留 modified 文件。
- 工作区存在文档层面遗留：A153–A155 轮次压缩产生的大量 deleted/modified/untracked 文件，尚未 git 暂存与提交。

## 2. 最近一轮结果（A157 / A157+）
- A157 为 reader 页面三个渲染路径（空态、无效章节、正常阅读）顶部添加 `DemoModeNotice` 静态横幅。已提交 commit `eecda3f`。
- A157+ 为补救轮次：确认 A157 commit 内容正确、横幅审查通过、typecheck 无 A157 特定错误。生成 `docs/rounds/codex/A157+_claude.md`。
- 横幅文案明确演示/预览模式、章节级进度恢复、无滚动位置、AI/RAG/provider 未启用。
- 修改 `apps/web/src/app/reader/page.tsx`（+18行）和 `apps/web/src/app/globals.css`（+33行）。
- 安全边界：纯静态 UI，未绑定任何开关、provider 调用或数据写入，未改业务逻辑。
- Typecheck 通过（已有环境错误为预存，非 A157 引入）。
- Lint 未执行（沙箱环境缺少 pnpm），判断无风险。
- 浏览器验证未执行（dev server 离线，Chrome 扩展未连接）。
- 无 reader 相关测试。

## 3. 下一轮 Codex 必读
- 本文件以及 `docs/codex-context/CODEX_ALWAYS_READ.md`、`SAFETY_BOUNDARIES.md`、`ARCHITECTURE_BOUNDARIES.md`。
- Reader 页面当前源码，理解已有 DemoModeNotice 横幅的位置和样式。

## 4. 下一轮 Codex 禁止
- 禁止真实 LLM 调用、工具执行、联网、保存 raw prompt/response。
- 禁止改动已稳定的 mock-only 边界（reader AI runtime、agent 示例等）。
- 禁止移除或弱化刚添加的 DemoModeNotice 横幅。
- 禁止修改 `package.json`、`pnpm-lock.yaml`、Prisma schema 或 migrations。
- 禁止修改 `docs/status/PROJECT_COMPLETION_SUMMARY.md`。
- 禁止擅自扩大 provider/tool/runtime 权限边界。

## 5. 安全边界
- 所有 Agent、Tool、LLM provider、Skill 能力均为 preview-only / mock-only / disabled-by-default。
- Reader Ask AI runtime 强制 `networkEnabled: false`，固定 mock-provider。
- Reader 顶部 DemoModeNotice 为静态 UI，不绑定真实能力。
- 任何页面不得暗示真实 AI 执行、真实数据闭环或真实工具运行。
- 禁止输出、硬编码 API key、密码、token、secret。

## 6. 文档处理分工
- Codex 每轮只维护最小文档：`Axxx_codex.md` / `Axxx+_claude.md` 和 `CURRENT_HANDOFF.md`。
- DeepSeek 负责阶段压缩、文档清理、`PROJECT_COMPLETION_SUMMARY.md` 更新、长文档生成。
- 当前遗留文档暂存/提交属于文�