<!-- Generated by DeepSeek doc agent at 2026-06-08T11:51:00 -->

# CURRENT_HANDOFF

<!-- Generated by DeepSeek doc agent at 2026-06-08T12:00:00 -->

## 1. 当前状态
- 主线：Web 网页端 + 软件端/Desktop；Skill 社区仅占位，不计入进度分母。
- Reader 同步预览链（A310–A338）：合约、就绪检查、进度预览、决策引擎、Prisma adapter、auth adapter、权限门 v1、幂等键 v1、真实 DB 集成测试、dev‑only 触发入口均以草案存在，全部标注“预览/禁用”，默认不写 DB。
- Desktop：只读卡片矩阵，数据仅存 localStorage；A338 新增静态首页只读“Reader Sync 健康状态”预览面板，暴露安全状态文案，不触发真实同步。
- Agent / Tool / Provider / Skill 能力仍为 preview‑only、mock‑only 或 disabled‑by‑default。
- 关键测试（lint、typecheck、Reader sync 单元/集成测试、Desktop GUI 回归）全部通过；真实 DB 集成测试仅在 opt‑in 环境可用。

## 2. 最近一轮结果（A338）
- **目标**：Desktop 静态首页注入只读“Reader Sync 健康状态”预览面板。
- **改动文件**：
  - `apps/desktop/local-reader-sync-health-panel.js`
  - `apps/desktop/local-reader-sync-health-panel.test.mjs`
  - `apps/desktop/main.js`
  - `apps/desktop/desktop-gui-regression.test.mjs`
  - (更新本交接文档和 Codex 轮次文档)
- **变更要点**：
  - 面板展示 12 个安全可见状态字段（`previewOnly: true`、`readiness: "disabled / preview-only"`、`databaseWrites: "disabled"` 等），文案明确标注“开发预览”、“只读状态”、“真实同步未连接”、“生产写入默认关闭”。
  - 面板只读，不写 DB，不接真实同步，不暴露敏感数据。
  - GUI 回归确认面板在 Desktop 首页可见。
- **验证结果**：lint ✅、typecheck ✅、面板单元测试 ✅、Desktop GUI 回归 ✅。
- **未变动**：无真实 auth 接入、无公开路由、无 schema/migration 变更、无生产 DB 写入路径。

## 3. 下一轮 Codex 必读
- `docs/codex-context/CURRENT_HANDOFF.md`（本文件）
- `docs/codex-context/CODEX_ALWAYS_READ.md`
- `docs/codex-context/CODEX_RULES.md`
- `docs/codex-context/SAFETY_BOUNDARIES.md`
- `docs/codex-context/ARCHITECTURE_BOUNDARIES.md`
- `docs/codex-context/DOC_WORKFLOW.md`
- 若任务涉及 Reader sync，额外必读 `docs/modules/reader-sync-current-state.md`（替代 A310‑A332 原始轮次文档）

## 4. 下一轮 Codex 禁止
- 禁止突破 preview‑only / mock‑only / disabled‑by‑default 边界。
- 禁止解锁生产环境真实 DB 同步，禁止删除 disabled 语义。
- 禁止接入真实 auth provider、暴露公开 API、修改数据库 schema/migration。
- 禁止在 UI 中将 preview 文案描述为真实能力。
- 禁止保存或输出 raw prompt、raw response、API key、token、secret。
- 禁止扩散到 Agent / Skill / Desktop 深层业务范围。
- 禁止超范围重构或顺手修无关问题。

## 5. 安全边界
- Agent / Runtime / Tool / Provider / Skill 全部仍为 preview‑only、mock‑only 或 disabled‑by‑default。
- 不得真实调用 LLM provider，不得真实执行工具。
- Reader 同步全链路标注“开发预览/禁用”，真实 DB 写入需多层显式 opt‑in 环境变量且生产默认关闭。
- Desktop 所有面板和卡片均为只读预览，不执行任何真实操作。
- UI 文案不得暗示真实执行、真实存储或已上线能力。

## 6. 文档处理分工
- **Codex**：专注写代码、修 bug、跑验证，输出 `docs/rounds/codex/Axxx_codex.md` 并更新本交接文档。**不承担长文档压缩、清理计划或阶段总结任务。**
- **DeepSeek**：定期（每 8‑12 轮）执行阶段压缩，更新 `docs/status/PROJECT_COMPLETION_SUMMARY.md` 和文档清理计划，Codex 不应在文档清理上持续花费多轮。

## 7. 下一步唯一建议
- **若继续 Desktop 主线**：可在同样只读边界内补全 Desktop 端的 Reader sync readiness gate 预览面板、dev‑only trigger 入口或本地测试工具，但严禁解锁真实同步和 auth。
- **若回归 Web 主线**：加固 Reader sync 的幂等冲突简单处理逻辑（仅限 dev/test‑only 流程），或修复已知类型/文案问题。
- **禁止**在任何模块中解锁生产环境同步、工具执行或 Agent 真实调用，除非用户单独开任务并完成安全前置条件。

## 8. 项目总进度
项目总进度：**58.00%**（截至 A338，口径：Web 网页端 + 软件端/Desktop，Skill 社区仅占位）
