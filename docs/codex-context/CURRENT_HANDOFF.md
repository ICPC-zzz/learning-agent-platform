<!-- Generated by DeepSeek doc agent at 2026-06-26T17:45:43 -->

好的，根据提供的资料，我生成了适用于下一轮 Codex 直接读取的短交接文档 `docs/codex-context/CURRENT_HANDOFF.md`。

---

<!-- Generated by DeepSeek doc agent at 2026-06-26T17:21:41 -->

# CURRENT_HANDOFF

## 1. 当前状态
- **项目方向**：AI Native 学习平台，四个模块 — 文章、题目、个人、AI 助手。
- **Web 端**：文章中心（占位）；Codeforces 2000 题最小元数据题库（搜索/筛选/分页/比赛倒计时）；个人 Codeforces 仪表盘（绑定/同步/统计/分析入口）；AI 助手代码分析面板（进度条/结构化报告/历史）。
- **Desktop**：只读预览壳，展示本地数据，无业务集成。
- **代码质量**：所有 6 个包 **typecheck 0 errors**；`next dev` 正常；5 个核心路由 HTTP 200。
- **安全**：所有 Agent/Tool/Provider/Skill/Runtime 能力均为 **preview-only / mock-only / disabled-by-default**；UI 标注“开发预览”；DB 写入默认关闭。
- **文档**：A500 已完成大规模文档清理，删除了 200+ 临时日志和 35+ 过期文档，保留精简结构。

## 2. 最近一轮结果（A500 — 文档压缩清理）
- **产出**：创建 `A394-A499_STAGE_SUMMARY`、重写 `PROJECT_COMPLETION_SUMMARY`（新口径 45-50%）、创建 `LEGACY_MODULES` 和 `DOCUMENT_INDEX`。
- **删除**：200+ 临时日志文件（~7.5 MB）、35+ 过期/重复文档（旧 books/Reader/OJ 方向）。
- **更新**：`CODEX_ALWAYS_READ` 和 `CURRENT_HANDOFF` 引用路径。
- **未动**：业务源码、DB、Git，未调用真实 LLM。
- **备份**：`E:\code\lap-a500-doc-cleanup-backup-20260626-092953`

## 3. 下一轮 Codex 必读
- 本次 `CURRENT_HANDOFF.md`。
- `docs/rounds/codex/A500_claude.md` — 本轮文档清理详细说明。
- `docs/status/PROJECT_COMPLETION_SUMMARY.md` — 最新项目完成度（新口径）。
- `docs/status/LEGACY_MODULES.md` — 废弃模块说明。
- **务必保持所有包 typecheck 0 errors**。

## 4. 下一轮 Codex 禁止
- 执行 `git add` / commit / push 等写入。
- 执行 `prisma db push` / migrate 或任何数据库写入。
- 调用真实 LLM provider 或执行真实工具。
- 将 mock / disabled 能力描述为真实上线功能。
- 大范围删除源码或重构 Prisma schema。
- 在 typecheck 被打破时继续开发新功能。

## 5. 安全边界
所有 Agent / Tool / Provider / Skill / Runtime 能力仍为 **preview-only / mock-only / disabled-by-default**。UI 必须明确标注“开发预览”。AI 助手默认 mock，真实调用需显式环境变量授权。DB 写入依赖多层环境变量 guard，默认关闭。

## 6. 文档处理分工
- **Codex**：写代码、修 bug、跑验证。每轮输出 `docs/rounds/codex/Axxx_claude.md` 并更新本文件第 1、2、7 节。
- **DeepSeek**：负责长文档压缩、文档整理、阶段总结，更新 `docs/status/PROJECT_COMPLETION_SUMMARY.md` 并生成本文件完整版本。
- 本次 A500 已由 Codex 执行一次性文档清理，后续大规模文档压缩仍由 DeepSeek 处理。

## 7. 下一步唯一建议
**A501：更新过期的源检查测试，使断言对齐当前产品范围。**
1. 修复 `a479-codeforces-problems-metadata.test.mjs`，适配当前 CF 元数据页。
2. 修复 `a484-curated-pool-and-agent-candidates.test.mjs` targetSize 断言，适配 full-pool-v2。
3. 修复 `a462-ai-assistant-page.test.mjs` 路径问题。
4. 修复 `user/page-source.test.mjs` 未登录 redirect 断言，适配当前可预览的个人中心。
5. 完成后运行所有包 typecheck，保持 0 errors。
6. **禁止**回滚当前产品行为以通过过期测试。
7. 完成后可继续推进：Auth v2 正式化 / CF API 真实回归 / 文章数据 pipeline / AI 模型安全接入。

## 8. 项目总进度
**项目总进度：45–50%**（口径：AI Native 学习平台四模块，旧 books/Reader/OJ 不计入主线分母。code existence ≠ 功能上线，mock 能力不计