<!-- Generated by DeepSeek doc agent at 2026-05-22T19:06:19 -->

# CURRENT_HANDOFF

## 1. 当前状态
- **主线**：Web 网页端 + Desktop 客户端；Skill 社区仅占位，不计入进度分母。
- **Web 页面安全审查**：`/books`、`/import`、`/reader`、`/learning`、`/agent` 五个页面文案均已标注 preview/mock/演示，边界固定。
- **Reader 本地能力集**：A157–A164 已实现 7 项纯前端能力，A165 已阶段性提交（commit hash 见 §2）：
  - DemoModeNotice 静态横幅
  - ReaderScrollPositionTracker 本地滚动位置保存/恢复
  - ReaderReadingTimer 本地阅读时长统计
  - ReaderChapterCompletionToggle 本章已读标记
  - ReaderScrollProgressIndicator 阅读进度百分比
  - ReaderChapterNavigation 上一章/下一章导航
  - ReaderVisibleBlockIndicator 当前可见块提示
  - **全部仅使用 localStorage / IntersectionObserver / Server Component，无数据库写入，无真实 AI 调用**
- **Desktop**：不可启动，为最大缺口。
- **Agent / Tool / LLM / Skill / Runtime**：全部 **preview-only / mock-only / disabled-by-default**。
- **工作区**：仍有约 40 个 deleted/modified/untracked 文件未暂存（不含业务逻辑）。

## 2. 最近一轮结果
**A165 — 阶段性提交 reader 本地能力集**
- 精确暂存并提交了 A157–A164 全部 reader 本地能力文件（9 个业务文件 + 2 个文档文件）。
- 提交前确认暂存区为空，无范围外文件泄露。
- typecheck 在 VM 环境下仅出现环境级 @types/react 缺失（TS2307/TS7026），reader 组件无语法/类型错误。A164 已确认 pnpm 环境下 typecheck 通过。
- lint 不可用（VM 无 pnpm），不阻塞。
- 未发现 reader 相关测试文件。
- commit hash：见下方 git log。

## 3. 下一轮 Codex 必读
- 本文件 `CURRENT_HANDOFF.md`
- `CODEX_RULES.md`、`SAFETY_BOUNDARIES.md`
- Reader 源码：`apps/web/src/app/reader/*.tsx`、`apps/web/src/components/reader/ReaderContent.tsx`
- 如需处理遗留文件，先执行 `git status`

## 4. 下一轮 Codex 禁止
- 禁止真实 LLM 调用、工具执行、联网、保存 raw prompt/response
- 禁止改动 reader AI runtime 的 mock-only 配置或任何 preview 边界文案
- 禁止修改 `package.json`、`pnpm-lock.yaml`、Prisma schema/migrations
- 禁止扩大 provider/tool/runtime 权限
- 禁止修改 `docs/status/PROJECT_COMPLETION_SUMMARY.md`（DeepSeek 维护）
- 禁止处理 `packages/ai-core` 下任何文件（除非用户明确授权）

## 5. 安全边界
- 所有 Agent/Tool/LLM/Skill 能力为 **preview-only / mock-only / disabled-by-default**
- Reader Ask AI runtime 强制 `networkEnabled: false`，固定 mock-provider
- UI 文案不得暗示真实 AI 执行、真实数据闭环或真实工具运行
- 禁止输出、硬编码任何密钥、密码、token、secret

## 6. 文档处理分工
- **Codex**：每轮维护 `Axxx_codex.md` + 本 `CURRENT_HANDOFF.md`；主要写代码、修 bug、跑验证
- **DeepSeek**：定期阶段压缩、文档整理、更新 `PROJECT_COMPLETION_SUMMARY.md`
- 遗留文件清理需用户确认后执行

## 7. 下一步唯一建议
Reader 本地能力集已提交。后续可选：

1. 推进 Desktop 最小可启动骨架（当前最大缺口）。
2. reader Ask AI 从 m