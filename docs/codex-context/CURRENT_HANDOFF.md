<!-- Generated by DeepSeek doc agent at 2026-05-23T16:51:20 -->

# CURRENT_HANDOFF

## 1. 当前状态
- **主线**：Web 网页端 + 软件端/Desktop client。Skill 社区仅为占位 scaffold，不计入进度分母。
- **Web 端**：Reader 本地预览能力已扩展至 13 项（阅读位置恢复、时长计时、已读标记、进度百分比、章节导航、块指示器、笔记草稿、字体调节、最近章节、阅读统计、书签等）。全部基于 localStorage / IntersectionObserver / CSS，不依赖数据库。
- **数据库**：Reader 最小 DB 同步代码（`syncScrollProgressAction`、`syncChapterCompletionAction`）已就位，但 **DATABASE_URL 从未在实际运行中配置，代码未经过端到端验证**。`seed-demo-user` 脚本与 `/api/dev/db-health` 路由同样未验证。
- **Desktop**：Electron 壳代码与静态产品入口首页已就绪，依赖（electron v34.5.8）已在宿主安装，安全审查通过（`nodeIntegration:false`, `contextIsolation:true`, `sandbox:true`），但 **Electron GUI 从未在本项目任何环境中成功启动**，不能视为可用客户端。
- **Agent**：所有 runtime、Tool、LLM provider、Skill 能力保持 **preview‑only / mock‑only / disabled‑by‑default**，无真实调用或执行。
- **工作区**：大量前序文档清理产生的 deleted/modified 文件仍**未暂存与提交**，`git status` 仍脏。

## 2. 最近一轮结果
- **上一轮 Codex（A177）**：为 Reader 增加了**本地书签/标记功能**（最多 30 条，smooth scroll，二次确认清空），UI 均标注“开发预览·仅保存在当前浏览器”。
- **DeepSeek**：执行了 A167–A177 的阶段压缩，更新了 `PROJECT_COMPLETION_SUMMARY.md`，项目总进度调整为 **36.00%**。

## 3. 下一轮 Codex 必读
- `docs/codex-context/CODEX_ALWAYS_READ.md`
- `docs/codex-context/CODEX_RULES.md`
- `docs/codex-context/SAFETY_BOUNDARIES.md`
- `docs/codex-context/ARCHITECTURE_BOUNDARIES.md`
- `docs/codex-context/DOC_WORKFLOW.md`
- **本文档**（`docs/codex-context/CURRENT_HANDOFF.md`）
- 最近一轮 Codex 总结（如 `docs/rounds/codex/A177_claude.md`）
- **不要**主动读取 `PROJECT_COMPLETION_SUMMARY.md`（DeepSeek 专属长文档），除非用户明确要求审计或阶段规划。

## 4. 下一轮 Codex 禁止
- ❌ **禁止修改文档**（尤其是 `PROJECT_COMPLETION_SUMMARY.md`、历史 round 文档、README 等），仅允许更新自身轮次总结和必要的代码注释。
- ❌ **禁止修改安全边界**：Agent/Tool/LLM 的 mock/disabled 标记、Reader 的安全提示、Electron 的安全配置等不得松动。
- ❌ **禁止真实 LLM 调用或真实工具执行**，所有 provider 保持 mock。
- ❌ **禁止修改 `packages/ai-core`、`learning-engine`、`book-engine` 的 preview 行为**，除非用户明确开启专项任务。
- ❌ **禁止删除或重命名 `PRODUCT_SPEC.md`、`SYSTEM_ARCHITECTURE.md`、`CURRENT_HANDOFF.md` 等核心文档**。

## 5. 安全边界
- 所有 Agent / Tool / Provider / Skill / Runtime 能力必须是 **preview‑only / mock‑only / disabled‑by‑default**，严禁描述为真实上线能力。
- Reader 所有 DB 同步标注为“开发预览”，失败时**静默回退至 localStorage**，不得报错或阻塞 UI。
- Reader 书签、笔记、统计等纯客户端数据必须**只保存在 localStorage**，不得发送到任何网络服务。
- Electron 主进程必须保持 `nodeIntegration: false`、`contextIsolation: true`、`sandbox: true`，不得引入 `nodeIntegration` 或 `preload` 的扩展能力。