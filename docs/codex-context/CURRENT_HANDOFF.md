<!-- Generated by Claude (A157) at 2026-05-22 -->

# CURRENT_HANDOFF

## 1. 当前状态
- 主线范围：Web 网页端 + Desktop 客户端。Skill 社区仅占位，不参与进度计算。
- Web 端页面（/books、/import、/reader、/learning、/agent）已完成安全边界审查、文案预览/演示标注及 mock‑only 配置，但 MVP 尚未闭环。
- **Reader 页面顶部已添加"演示模式"静态通知横幅**（A157），明确告知用户当前为预览/演示状态，进度仅章节级、无滚动位置恢复、AI/RAG/provider 未启用。
- Desktop 客户端不可启动，无实质完成。
- 所有 Agent / Tool / LLM provider / Skill / Runtime 能力仍为 preview‑only / mock‑only / disabled‑by‑default。
- `packages/ai-core` 4 个遗留 modified 文件已在 A156 审查并提交，均为安全的 import 清理/死代码移除/风格调整。工作区 ai-core 代码层面已干净。
- 工作区仍有文档层面遗留：A153/A154/A155 的 modified 文档、A155 压缩产生的 deleted 文件和 untracked 压缩报告，尚未 git 暂存/提交。

## 2. 最近两轮结果
- **A157**：为 reader 页面三个渲染路径（空态、无效章节、正常阅读）顶部统一添加 `DemoModeNotice` 静态横幅。文案明确说明演示/预览模式、章节级进度恢复、无滚动位置、AI/RAG/provider 未启用。修改 `apps/web/src/app/reader/page.tsx`（+18行）和 `apps/web/src/app/globals.css`（+33行）。未改 reader 业务逻辑，未接 provider/RAG/AI，未新增依赖。typecheck 通过（环境相关错误均为预存，与改动无关）。浏览器验证未执行（dev server 离线、Chrome 扩展未连接）。已暂存待提交。
- A156：审查 `packages/ai-core` 下 4 个遗留 modified 文件。全部判定为安全清理，无安全边界突破。已 typecheck 通过，已提交。commit: `chore(ai-core): resolve preview provider leftovers`。
- A155：生成工作区遗留文件分类清单，未执行任何 git 操作。
- A148‑A154：完成 reader 阅读进度稳定化、reader AI runtime 固定为 mock‑only、learning 状态对齐、agent 文案收紧、books/import 预览边界标注等安全边界约束，全部已提交，进度保持 30.00%。

## 3. 下一轮 Codex 必读
- 本文件以及 `docs/codex-context/CODEX_ALWAYS_READ.md`、`SAFETY_BOUNDARIES.md`、`ARCHITECTURE_BOUNDARIES.md`。
- Reader 页面当前源码，理解已有 DemoModeNotice 横幅位置和样式。

## 4. 下一轮 Codex 禁止
- 禁止真实 LLM 调用、工具执行、联网、保存 raw prompt/response。
- 禁止改动已稳定的 mock‑only 边界（reader AI runtime、agent 示例等）。
- 禁止擅自扩大 provider/tool/runtime 权限边界。
- 禁止修改 `docs/status/PROJECT_COMPLETION_SUMMARY.md`。
- 禁止修改 `package.json`、`pnpm-lock.yaml`、Prisma schema/migrations。
- 禁止移除或弱化刚添加的 DemoModeNotice 横幅。

## 5. 安全边界
- 所有 Agent、Tool、LLM provider、Skill 能力均为 preview‑only / mock‑only / disabled‑by‑default。
- reader Ask AI runtime 强制 `networkEnabled: false`，固定 mock‑provider，即使环境变量存在也不走真实调用。
- **Reader 页面顶部 DemoModeNotice 横幅为静态 UI**，不绑定任何开关/provider/后台任务。
- 任何页面不得暗示真实 AI 执行、�